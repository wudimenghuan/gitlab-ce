%ul.content-list.pipelines
  - if pipelines.blank?
    %li
      .nothing-here-block 没有管道可以显示
  - else
    .table-holder
      %table.table.ci-table
        %tbody
          %th 状态
          %th 管道
          %th 提交
          %th 阶段
          %th
          %th
        = render pipelines, commit_sha: true, stage: true, allow_retry: true, stages: pipelines.stages, show_commit: false
