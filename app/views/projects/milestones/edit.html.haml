- @no_container = true
- page_title "编辑", @milestone.title, "里程碑"
= render "projects/issues/head"

%div{ class: container_class }

  %h3.page-title
    编辑里程碑 ##{@milestone.iid}

  %hr

  = render "form"
