.js-toggle-container
  .commit-stat-summary
    正在显示
    = link_to '#', class: 'js-toggle-button' do
      %strong #{pluralize(diffs.count, "个修改的文件", "个修改的文件")}
    - if current_controller?(:commit)
      - unless @commit.has_zero_stats?
        包含
        %strong.cgreen #{@commit.stats.additions} 行增加
        和
        %strong.cred #{@commit.stats.deletions} 行删除
  .file-stats.js-toggle-content.hide
    %ul
      - diffs.each_with_index do |diff, i|
        %li
          - if diff.deleted_file
            %span.deleted-file
              %a{href: "#diff-#{i}"}
                %i.fa.fa-minus
                = diff.old_path
          - elsif diff.renamed_file
            %span.renamed-file
              %a{href: "#diff-#{i}"}
                %i.fa.fa-minus
                = diff.old_path
                &rarr;
                = diff.new_path
          - elsif diff.new_file
            %span.new-file
              %a{href: "#diff-#{i}"}
                %i.fa.fa-plus
                = diff.new_path
          - else
            %span.edit-file
              %a{href: "#diff-#{i}"}
                %i.fa.fa-adjust
                = diff.new_path

