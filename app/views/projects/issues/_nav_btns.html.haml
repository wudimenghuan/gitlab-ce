= link_to params.merge(rss_url_options), class: 'btn btn-default append-right-10 has-tooltip', title: '订阅' do
  = icon('rss')
- if @can_bulk_update
  = button_tag "编辑问题", class: "btn btn-default append-right-10 js-bulk-update-toggle"
= link_to "新建问题", new_project_issue_path(@project,
                                              issue: { assignee_id: finder.assignee.try(:id),
                                                       milestone_id: finder.milestones.first.try(:id) }),
                                              class: "btn btn-new",
                                              title: "新建问题",
                                              id: "new_issue_link"
