- page_title "代码片段"

.sub-header-block
  - if can?(current_user, :create_project_snippet, @project)
    = link_to new_namespace_project_snippet_path(@project.namespace, @project), class: "btn btn-new btn-wide-on-sm pull-right", title: "新建代码片段" do
      新建代码片段

  .oneline
    在 git 版本仓库外共享代码

= render 'snippets/snippets'
