= form_errors(hook)

.form-group
  = form.label :url, 'URL', class: 'label-bold'
  = form.text_field :url, class: 'form-control'
.form-group
  = form.label :token, '安全令牌', class: 'label-bold'
  = form.text_field :token, class: 'form-control'
  %p.form-text.text-muted
    使用此令牌来验证接收信息的有效性
.form-group
  = form.label :url, '触发器', class: 'label-bold'
  %ul.list-unstyled
    %li
      .form-text.text-muted
        系统钩子将在创建项目和增加 SSH 密钥等事件发生时触发。
        你可以额外开启推送等事件的触发。

    .prepend-top-default
      = form.check_box :repository_update_events, class: 'float-left'
      .prepend-left-20
        = form.label :repository_update_events, class: 'list-label' do
          %strong 版本库更新事件
        %p.light
          此链接将在版本库每次更新时触发
    %li
      = form.check_box :push_events, class: 'float-left'
      .prepend-left-20
        = form.label :push_events, class: 'list-label' do
          %strong 推送事件
        %p.light
          此链接将在推送新版本到版本库时触发
    %li
      = form.check_box :tag_push_events, class: 'float-left'
      .prepend-left-20
        = form.label :tag_push_events, class: 'list-label' do
          %strong 标签推送事件
        %p.light
          此链接将在推送新标签到版本仓库时触发
    %li
      = form.check_box :merge_requests_events, class: 'float-left'
      .prepend-left-20
        = form.label :merge_requests_events, class: 'list-label' do
          %strong 合并请求事件
        %p.light
          此链接将在合并请求创建、更新或合并时触发
.form-group
  = form.label :enable_ssl_verification, 'SSL 验证', class: 'label-bold checkbox'
  .form-check
    = form.check_box :enable_ssl_verification, class: 'form-check-input'
    = form.label :enable_ssl_verification, class: 'form-check-label' do
      %strong 启用 SSL 证书验证
