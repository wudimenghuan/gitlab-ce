- page_title '滥用举报'
%h3.page-title 滥用举报
%hr
.abuse-reports
  - if @abuse_reports.present?
    .table-holder
      %table.table.responsive-table
        %thead.hidden-sm.hidden-xs
          %tr
            %th 被举报用户
            %th 举报人
            %th.wide 信息
            %th 操作
        = render @abuse_reports
    = paginate @abuse_reports, theme: 'gitlab'
  - else
    .empty-state
      .text-center
        %h4 没有滥用举报！ #{emoji_icon 'tada'}
