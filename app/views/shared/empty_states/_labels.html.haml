.row.empty-state.labels
  .col-xs-12
    .svg-content
      = render 'shared/empty_states/icons/labels.svg'
  .col-xs-12.text-center
    .text-content
      %h4 标记可应用于问题和合并请求的分组。
      %p 您还可以为标记加注星标，使其成为优先标记。
      - if can?(current_user, :admin_label, @project)
        = link_to '新建标记', new_namespace_project_label_path(@project.namespace, @project), class: 'btn btn-new', title: '新建标记', id: 'new_label_link'
        = link_to '生成默认标记组', generate_namespace_project_labels_path(@project.namespace, @project), method: :post, class: 'btn btn-success btn-inverted', title: '生成默认标记组', id: 'generate_labels_link'
