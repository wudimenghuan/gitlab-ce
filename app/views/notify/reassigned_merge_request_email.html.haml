重新分配合并请求 #{ @merge_request.iid }

= url_for([@merge_request.project.namespace.becomes(Namespace), @merge_request.project, @merge_request, { only_path: false }])

指派变更
- if @previous_assignee
  从 #{@previous_assignee.name}
变更为
= @merge_request.assignee_id ? @merge_request.assignee_name : '未指派'
