重新指派合并请求 <%= @merge_request.iid %>

<%= url_for([@merge_request.project.namespace.becomes(Namespace), @merge_request.project, @merge_request, { only_path: false }]) %>

委托人变更为 <%= "from #{@previous_assignee.name}" if @previous_assignee -%>
 to <%= "#{@merge_request.assignee_id ? @merge_request.assignee_name : '未指派'}" %>
