= render 'devise/shared/tab_single', tab_title: '重新发送确认邮件'
.login-box
  .login-body
    = form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: 'gl-show-field-errors' }) do |f|
      .devise-errors
        = devise_error_messages!
      .form-group
        = f.label :email, "邮箱"
        = f.email_field :email, class: "form-control", required: true, title: '请提供一个有效的邮箱地址'
      .clearfix
        = f.submit "重新发送确认邮件", class: 'btn btn-success'

.clearfix.prepend-top-20
  = render 'devise/shared/sign_in_link'
