- content_for :header do
  %h1{style: "background: #c40834; color: #FFF; font: normal 20px Helvetica, Arial, sans-serif; margin: 0; padding: 5px 10px; line-height: 32px; font-size: 16px;"}
    GitLab CI (构建失败)
%h3
  项目：
  = link_to ci_project_url(@project) do
    = @project.name

%p
  提交链接：#{link_to @build.short_sha, namespace_project_commit_path(@build.gl_project.namespace, @build.gl_project, @build.sha)}
%p
  作者：#{@build.commit.git_author_name}
%p
  分支：#{@build.ref}
%p
  阶段：#{@build.stage}
%p
  作业：#{@build.name}
%p
  信息：#{@build.commit.git_commit_message}

%p
  构建明细：#{link_to "构建 #{@build.id}", namespace_project_build_url(@build.gl_project.namespace, @build.gl_project, @build)}
